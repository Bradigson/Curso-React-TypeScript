creamos un componente con el nombre del parametro
[id].js
esto debe de ir dentro de una cartpeta que tenga el mismo nombre que le pusimos
al componente [id].js




1- comoponente [id].js

/page
/view
[id].js



import { useRouter } from "next/router"
const View = ()=>{
    const router:any = useRouter();
    const {id} = router.query;
    console.log(id);
    return(
        <div>
            el id del usuario es : {id}
        </div>
    )
}


export default View;










2-
 <Link href={`/view/5`}>
        Go To Id
      </Link>










3-
import { useRouter } from 'next/router';

const Home = () => {
  const router = useRouter();

  const handleNavigation = () => {
    // Navegar a otra página, por ejemplo, /about
    router.push('/about');
  };

  return (
    <div>
      <h1>Home Page</h1>
      <button onClick={handleNavigation}>Ir a About</button>
    </div>
  );
};

export default Home;
